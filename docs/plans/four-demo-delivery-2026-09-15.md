# Four-demo meetup delivery

- `Tease:` One friendly kit, four working demos.
- `Lede:` Finish the four-demo application in the existing meetup repository, deploy four direct URLs, verify a Google AI Studio import, and make a linked Notion kit the main attendee entry point.
- `Why it matters:` Attendees can begin in Notion, try an example, and reach the source and rebuild instructions without starting in GitHub.
- `Go deeper:` Track delivery and actual verification below; prior context is in the September 14 consolidated research.

## Original prompt

Sam requested the existing Context, RAG, FunctionGemma fine-tuning, and Rust tiny-model demos working first, with four public URLs and verified Google AI Studio imports. He confirmed the previous meetup repository, agent execution as soon as possible this week, and the proposed finish line. On September 15 he added: “I want the Notion doc to be the entry point, the main entry point for this meetup.” GitHub and the kit must link to each other.

## Goal and decision

Use `techlahoma/techlahoma-google-apps-starter`. Preserve one application with shared navigation and four deep links. Use AI Studio's supported GitHub import and exercise every demo after import. Keep attendee files local. Retain the approved dark, chat-first layout with progressive disclosure of corpus, evaluation, and build instructions.

The terminal outcome includes reviewed commits, push, deployment, and a shareable Notion kit. These effects are authorized by the confirmed finish line. Verify exact targets before performing them. No unrelated app, event, sponsor, or community edits are included.

## Non-goals

WordPress crossover is tentatively October. Fly simulation is a stretch goal. Neither is part of this delivery. Do not rewrite the existing research corpus or publish private source indexes, contact records, or sponsorship notes.

## Rollout plan

1. Recover cloud code if it exists on an accessible ref; otherwise record the gap and build from settled requirements. Establish locked dependencies and a real browser baseline.
2. Complete shared shell, local corpus handling, Context, and actual semantic/hybrid RAG. Preserve model and data provenance and honest no-match states.
3. Complete real FunctionGemma training and tiny-model Rust training. Browser LoRA requires 12 successful windowless hardware GPU runs; a verified CLI fallback remains required. Distinguish training, inference, prepared adapters, and replay.
4. Run app completion and repository checks, review the scoped diff, commit/push, deploy to the exact app Hosting target, and verify all four public deep links.
5. Import through AI Studio and verify all four demos there. Create the attendee Notion kit, add the reciprocal GitHub link, and check anonymous kit access and every attendee path.

## Validation gates

- Baseline and final: `bun run setup:doctor`, `bun run verify`.
- App: app-local check and `bun run app:verify --app building-ai-models-without-coding` once that workspace exists.
- Chromium desktop and phone-sized paths are separate from real device/GPU proof.
- No invented model outputs, training curves, benchmarks, compatibility claims, URLs, or import receipts.
- Each demo must expose its real user-visible interaction, error/empty/loading paths, provenance, and supported fallback.
- Final evidence records source SHA, deployment target, four URLs, AI Studio import result, Notion link/access, and remaining limitations.

## Progress and open evidence

- 2026-09-15: User confirmed scope and Notion-first kit.
- Worktree starts at `f3897cece931bac8233edb5e4b50379429b08132`, with only two prior untracked research documents. Main Mac checkout has unrelated dirty work and is read only.
- Git 2.50.0 and Bun 1.3.14 available. Locked dependency installation started; no browser launch proved yet.
- GitHub recovery agent reports no four-demo branch/PR and no commit `f4be29f` on the remote. Cloud transcript exposes an older partial result only. Recovery inspection continues.
- Notion agent located August kit under an August-specific workshop parent. No September attendee kit found in focused search. New kit should not be nested beneath the old event.
- FunctionGemma browser LoRA and editable Rust compilation remain real engineering questions. No success or timing claim yet.

### Focus interruption and corrected browser mode

- Sam stopped execution because new Chrome windows were taking device focus. He requested research of prior Google Ads/other project practice, then continuation without that interruption.
- The recovered Ads-related analytics test was headless Playwright. The WordPress July 28 diagnosis distinguishes ordinary headed-launch focus activation from the July 25 node_repl LaunchServices crash incident. No verified hidden-headed workaround was recovered.
- Browser proof scripts now share `scripts/browser-runtime.ts`: full Chromium, unified headless, browser sandbox enabled. No unsafe GPU flags or new headed window fallback.
- Actual windowless probe passed on Chrome 151.0.7922.34 with Apple metal-3 and a compute result of 42. This confirms hardware GPU availability, not all model compatibility.
- The prior headed 12-run wording is superseded by Sam's non-focus constraint. Retain 12 real end-to-end hardware GPU training runs, using windowless Chromium. If that mode cannot execute a required feature, use the connected existing Chrome instance through the browser integration.
- Interrupted owned test browsers were cleaned up; existing user Chrome windows were left untouched.

### Delivery checkpoint

- Source commits `ea2de1d` and `bbc110b` pushed to the existing remote main and matching remote SHA verified.
- `gdg-model-workshop` deployed. All four deep links passed live checks; real Context/RAG, Rust compile/train, and one complete browser LoRA run verified on the public site.
- Twelve consecutive hardware-GPU training runs passed with identical saved adapter hashes; default held-out quality remains 2/3. The other two task fixtures run but each scored 1/3.
- Notion kit published and opened anonymously. The kit links to all four demos and GitHub; repository/app links point back to the public kit.
- AI Studio Import from GitHub remains at the user's account sign-in; no import success is claimed. The tab is preserved for user handoff.
- CI repairs are under verification. The complete-history scanner's existing broad Google browser-key finding is left unresolved and unsuppressed; no shared credential was changed.
